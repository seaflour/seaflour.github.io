{
  "name": "www.seaflour.net",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf www && ELEVENTY_ENV=prod eleventy",
    "serve": "rm -rf www && ELEVENTY_ENV=dev eleventy --serve",
    "update": "git pull && npm i && npm run build"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
